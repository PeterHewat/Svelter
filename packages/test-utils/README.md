# @repo/test-utils

Shared test utilities for the Svelter monorepo.

## Exports

- `@repo/test-utils/convex-svelte-setup` — Vitest mocks for `convex-svelte` (no live deployment in unit tests)

Import in `apps/web` Vitest setup when testing components that use Convex hooks.
